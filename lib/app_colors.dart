import 'package:flutter/material.dart';

// Central color palette for the app.
// Edit these values if your group wants to change the overall Appstone theme.
class AppColors {
  static const Color primary = Color(0xFF8B1A1A);
  static const Color primaryDark = Color(0xFF6B1414);
  static const Color grey = Color(0xFF5A5A5A);
  static const Color greyDark = Color(0xFF3D3D3D);
  static const Color gold = Color(0xFF8B7020);
  // Single "danger" tone for destructive actions and time's-up alerts. A deep
  // red that reads as urgent while still sitting inside the warm maroon theme -
  // use this instead of raw Colors.red so every warning/delete looks the same.
  static const Color danger = Color(0xFFC62828);
  static const Color background = Color(0xFFF2F0ED);
  static const Color white = Colors.white;
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textGrey = Color(0xFF888888);
}
